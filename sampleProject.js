module.exports = {
    basePrompt: "A {emotion} girl named {name} with {hair_color} hair",
    negative_prompt: "AC_Neg1",
    steps: 20,
    guidance_scale: 4
    // guidance_scale is not specified, so it should use the default value
};